/**
 * Give Item keyboard shortcuts configuration.
 * Change these keys to whatever you prefer.
 */
export const GIVE_ITEM_KEYS = {
    /** Key to ACCEPT an incoming item request */
    accept: 'y',
    /** Key to DECLINE an incoming item request */
    decline: 'n',
} as const;
